export enum HandoffTarget {
  /** Auto-assign via CRM routing rules */
  GENERAL = "general",
  /** Assign to a specific CRM group/team */
  GROUP = "group",
  /** Assign to a specific CRM agent */
  AGENT = "agent",
}
